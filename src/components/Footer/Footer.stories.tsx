import React, { FC } from "react";
// Components
import Footer from "./Footer";

export default {
  tags: ["autodocs"],
  title: "Components/Layout/Footer",
  component: Footer,
};

export const Default: FC = () => {
  return <Footer>Renders a footer element.</Footer>;
};
